/* exported getLastChar */
/* get the last character of a string using a function that passes
the string as an argument. return the last index by using the strings length - 1 */
function getLastChar(string) {
  return string[string.length - 1];
}
