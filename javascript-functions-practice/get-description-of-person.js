/* exported getDescriptionOfPerson */
function getDescriptionOfPerson(person) {
  var description = person;
  return description.name + ' is a ' + description.occupation + ' from ' + description.birthPlace + '.';
}
